import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  titleConfirmation: {
    textAlign: "center",
    fontSize: 20,
    marginBottom: 10,
    fontWeight: "500",
    color: "#f094c0",
  },
});

export default styles;
