import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  headerStyle: {
    height: 130,
    backgroundColor: "#fff",
  },
  titleHeader: {
    fontSize: 40,
    color: "#339dd7",
  },
});

export default styles;
